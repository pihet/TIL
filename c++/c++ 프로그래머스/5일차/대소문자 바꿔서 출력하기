#include <iostream>
#include <string>

using namespace std;

int main(void){
    string str;
    cin >> str;

    for (char &c : str){
        if(isupper(c)){
            cout << (char)tolower(c);
        }
        else
            cout << (char)toupper(c);
    }return 0;

}

/*
영어 알파벳으로 이루어진 문자열 str이 주어집니다. 각 알파벳을 대문자는 소문자로 소문자는 대문자로 변환해서 출력하는 코드를 작성해 보세요.
tolower()와 toupper() 함수는 문자에 대한 ascii 값을 기반으로 해서 문자를 처리하기 위해 해당 문자를 char 타입으로 변환해야한다.


islower(c): 문자가 소문자인지 확인합니다.
toupper(c): 문자가 소문자일 때, 대문자로 변환합니다.
isupper(c): 문자가 대문자인지 확인합니다.
tolower(c): 문자가 대문자일 때, 소문자로 변환합니다.

*/