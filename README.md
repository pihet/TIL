# Programmers 코딩테스트 학습 기록장

> 단순히 문제 풀이 코드를 저장하는 것을 넘어, 풀이 과정에서의 아이디어, 자료구조/알고리즘 개념, 배운 점을 의미 있게 기록합니다.

---

## 최종 목표 (2달 커리큘럼)
- **최종 목표**: 2달 이내 프로그래머스 Level 3 혼자 풀기 + Level 4 도전 가능 수준 도달 (Python 알고리즘 및 SQL 동시 진행)
- **현재 수준**: 
  - Python 알고리즘: Level 1 ~ Level 2 (스택/큐, 탐욕법, 정렬 진행 중)
  - SQL: Level 3 ~ Level 4 문제 집중 정복 진행 중

---

## 학습 가이드라인 & AI 멘토 규칙

- **문제 출처**: 반드시 프로그래머스(Programmers)에 실제로 존재하는 문제로 추천 및 진행합니다.
- **폴더 및 파일 저장 규칙**:
  - Python: `python/N일차/N일차.ipynb` 및 `python/N일차/N일차_오답노트.md`
  - SQL: `sql/N일차/N일차.ipynb` 및 `sql/N일차/N일차_오답노트.md`
- **중복 문제 추천 금지 규칙**: 이미 풀었거나 README.md 학습 현황에 기록된 문제는 절대 다시 추천하지 않으며, 항상 이전에 다루지 않은 새로운 문제로만 구성하여 제공합니다.
- **SQL 문제 난이도 규칙**: SQL 문제는 기본적으로 **Level 3 ~ Level 4 난이도**로 제공합니다.
- **SQL 예시 테이블 및 결과 포함 규칙**: 지문 내에 예시로 제공되는 테이블 구조(컬럼 및 샘플 데이터)와 기대되는 최종 출력 결과 테이블을 마크다운 표(Table) 형태로 반드시 수록합니다.
- **정답 및 뼈대 코드 최소화 규칙**: 스타터 코드는 기본 함수 껍데기(`def solution(...)`, `SELECT`)만 제공하고 정답 코드는 작성해두지 않습니다.
- **매일 오후 11:50 자동 Git Commit & Push 규칙**: 매일 오후 11:50(23:50 KST) 또는 사용자 요청 시 GitHub에 자동 푸시합니다.

---

## 프로그래머스 약점 및 복습용 종합 오답 노트 (지속 업데이트)

### 📌 Python 알고리즘 복습 노트
- **Python 9일차 2번 (K번째수)**:
  - `list.append(a, b)`처럼 2개 이상의 인자를 전달하여 `TypeError` 발생 $\rightarrow$ 구하고자 하는 단일 값만 `append()`할 것.
  - 1-based 인덱싱과 0-based 인덱싱 차이 (`i`번째 $\rightarrow$ `i-1` 인덱스) 유의.
- **Python 9일차 3번 (가장 큰 수)**:
  - 문자열 비교 시 자릿수가 다를 경우 단순 내림차순 정렬하면 오류 발생 (예: `'3'`과 `'30'`) $\rightarrow$ 문자열 3회 반복(`x * 3`) 비교 정렬 패턴 활용 (`'333'` > `'303030'`).
  - `list.sort()`는 제자리 정렬로 `None`을 반환하므로 `"".join(list.sort())` 형태로 직접 사용 불가.
- **Python 10일차 1번 (큰 수 만들기)**:
  - `range(str_var)` 사용 시 `TypeError` 발생 $\rightarrow$ `for num in number:` 문자열 direct 순회 활용.
  - `stack[0]`은 바닥 원소 $\rightarrow$ 스택 상단 비교 시 `stack[-1]` 활용.
  - 다중 연속 제거 시 `if` 대신 `while stack and stack[-1] < num and k > 0:` 활용.
  - 내림차순 등으로 `k`가 남아있을 경우 `stack[:-k]` 슬라이싱 예외 처리.
- **Python 10일차 2번 (같은 숫자는 싫어)**:
  - `set()` 적용 시 연속 중복이 아닌 전체 중복 제거 및 순서 파괴 발생 $\rightarrow$ `if not answer or answer[-1] != i` 단락 평가 `or` 조건문 활용.
- **Python 10일차 3번 (기능개발)**:
  - 날짜 계산 시 `round()` 반올림 사용 오류 $\rightarrow$ `math.ceil()` 올림 활용.
  - `max_day` 기준일 및 `count` 변수를 활용한 그룹화 배포 조건 작성.
- **Python 11일차 1번 (타겟 넘버)**:
  - **변수 오타 및 스택 인덱스 전진**: `answre` 오타 및 `i-1` 인덱스 감점 오답 발생 $\rightarrow$ `-` 연산 시에도 다음 단계로 넘어가므로 동일하게 `i+1` 전진 적용.
  - **탐색 지속성 판단**: `j == target` 도달 시 해당 경로만 완료될 뿐 스택에 남아있는 다른 갈림길 경로 탐색을 계속 진행함.
- **Python 11일차 2번 (더 맵게)**:
  - **힙(Heap) 활용 및 예외 처리**: 매번 `list.sort()` 사용 시 시간 초과 발생 $\rightarrow$ `heapq` 활용. 원소가 1개 남았을 때 2개 추출 시 `IndexError` 방지를 위해 `if len(scoville) < 2: return -1` 예외 처리 필수.
- **Python 11일차 3번 (단속카메라)**:
  - **그리디 람다 정렬 및 탐색**: `sorted(routes, key=lambda x: x[1])`로 진출 지점 오름차순 정렬 후, 가장 먼저 나가는 차량의 진출 지점에 첫 카메라 설치 (`camera = routes[0][1]`, `count = 1`). `route[0] > camera`인 경우에만 새 카메라 설치.
- **Python 11일차 4번 (게임 맵 최단거리)**:
  - **변수 덮어쓰기 및 들여쓰기 유의**: `maps = [(0,0)]` 처럼 데이터 덮어씌움 오타 주의 (`maps[0][0] = 0` 방지). 4방향 유효성 검사 `if`문은 `for i in range(4):` 문 내부 들여쓰기 필수.
- **Python 12일차 2번 (네트워크)**:
  - **할당(`=`)과 비교(`==`) 연산자 혼동 시 무한 재귀 발생**: `visited[i] = True`가 아닌 `visited[i] == True`로 표기하여 방문 처리가 되지 않고 무한 재귀(`RecursionError`) 유발 $\rightarrow$ 변수 변경 시 `=` 등호 1개 사용 확인.
- **Python 15일차 2번 (무인도 여행)**:
  - **격자 탐색 런타임 에러(재귀 깊이 초과) 방지**: 100x100 격자 등 탐색 규모가 클 때는 재귀 깊이가 10,000을 넘을 수 있으므로 `sys.setrecursionlimit(100000)` 설정 또는 스택(`while stack:`) 반복문 DFS로 구현할 것.
  - **좌표 유효성 검사**: `0 <= nx < r and 0 <= ny < c` 경계값 검사 정확히 지정.

---

### 📌 SQL 트랙 복습 노트
- **SQL 1일차**:
  - `DATEDIFF` 시 시작일 포함을 위해 반드시 `+ 1` 처리할 것 (`DATEDIFF(END_DATE, START_DATE) + 1`).
  - `CASE WHEN` 조건 끝에 `END` 사용 필수, 컴마(`,`) 금지.
- **SQL 7일차 1번 (오랜 기간 보호한 동물(2))**:
  - **`WHERE` 절 집계 함수 사용 불가**: `WHERE` 절에는 `MAX()` 같은 집계 함수를 쓸 수 없으며, 상위 N개 추출 시 `ORDER BY DATEDIFF(...) DESC LIMIT 2` 조합 활용할 것.
  - **함수명 및 오타**: MySQL 날짜 일수 차이는 `DATE_DIFF`가 아닌 `DATEDIFF(종료일, 시작일)` 사용.
  - **컬럼명 및 모호성**: 테이블의 이름 컬럼은 `ANIMAL_NAME`이 아니라 `NAME`이며, 다중 테이블 조인 시 `A.ANIMAL_ID` 처럼 테이블 별칭 필수 명시.
  - **정렬 방향**: "가장 길었던" 조건은 오름차순(`ASC`)이 아닌 내림차순(`DESC`) 적용.
- **SQL 7일차 2번 (식품분류별 가장 높은 가격의 식품 정보 조회하기)**:
  - **`GROUP BY` 기준 오설정**: `PRODUCT_ID`로 그룹화 시 개별 행이 출력됨 $\rightarrow$ `CATEGORY`로 그룹화할 것.
  - **그룹화 시 타 컬럼 불일치 (Lv.4 핵심)**: `GROUP BY CATEGORY`를 한 뒤 `PRODUCT_NAME`을 그냥 뽑으면 최고가 상품명이 아닌 임의의 상품명이 반환됨 $\rightarrow$ `WHERE (CATEGORY, PRICE) IN (서브쿼리)` 패턴 필수 적용.
  - **문자열 조건 오타**: `IN ("'과자', '국'...")` 처럼 전체를 따옴표로 묶으면 1개의 단어로 인식됨 $\rightarrow$ `IN ('과자', '국', '김치', '식용유')` 개별 문자열 분리할 것.
- **SQL 7일차 3번 (취소되지 않은 진료 예약 조회하기)**:
  - **다중 테이블 조인 시 컬럼 모호성(Ambiguous)**: `PATIENT`, `APPOINTMENT`, `DOCTOR` 3개 테이블 조인 시 `PT_NO`, `MCDP_CD` 등 공통 컬럼은 `B.PT_NO`, `C.MCDP_CD` 처럼 테이블 별칭 필수 명시.
- **SQL 8일차 1번 (주문량이 많은 아이스크림들 조회하기)**:
  - **문자열 컬럼 집계 연산 불가**: `SUM(FLAVOR)`처럼 문자열 컬럼에 `SUM()`을 집계하면 안 되며, 수량 컬럼인 `TOTAL_ORDER`(`SUM(A.TOTAL_ORDER + B.TOTAL_ORDER)` 등)를 집계해야 함.
  - **1:N 조인 시 `GROUP BY` 필수**: `JULY` 테이블에는 동일한 `FLAVOR` 데이터가 복수로 수록되므로, 맛별 합산을 위해 `GROUP BY FLAVOR`가 필수적임.
  - **정렬 방향**: "주문량이 많은 순서"는 `ASC`가 아닌 **`DESC`** 내림차순을 적용해야 함.
- **SQL 8일차 2번 (카테고리 별 도서 판매량 집계하기)**:
  - **서브쿼리 조인 키 누락**: 서브쿼리 조인 연산 시 `BOOK_ID`를 SELECT 목록에 포함해야 ON 조건 결합 가능.
  - **날짜 추출 연산**: 2022년 1월 추출 시 `SALES_DATE LIKE '2022-01%'` 또는 `DATE_FORMAT(SALES_DATE, '%Y-%m') = '2022-01'` 활용.
  - **카테고리별 집계**: 개별 도서 단위 조인 후 `GROUP BY A.CATEGORY` 및 `SUM(B.SALES)` 처리.
- **SQL 8일차 3번 (대여 횟수가 많은 자동차들의 월별 대여 횟수 구하기)**:
  - **서브쿼리 HAVING 분리 필수**: 메인 쿼리에 `HAVING COUNT(*) >= 5` 적용 시 월별 대여 횟수가 5회 이상인 월만 출력되는 오류 발생. 전체 기간 총 5회 이상 차량 선별을 위해 서브쿼리 `HAVING COUNT(*) >= 5` 패턴 필수.
  - **문법 및 구분자 오타 주의**: `BETWEEN ... AND` 구문 및 `ORDER BY` 절 콤마(`,`) 구분자 사용.
- **SQL 9일차 1번 (헤비 유저가 소유한 장소)**:
  - **`WHERE` 절 `IN` 연산자 누락**: `WHERE (서브쿼리)` 구문 작성 시 연산자 미작성 구문 오류 $\rightarrow$ `WHERE HOST_ID IN (서브쿼리)` 패턴 필수.
  - **서브쿼리 `SELECT` 컬럼 단일화**: 서브쿼리에서 `GROUP BY HOST_ID` 시 `SELECT ID, HOST_ID2` 형태로 오타 및 불필요한 컬럼 작성 $\rightarrow$ 비교 키인 `SELECT HOST_ID` 단일 컬럼만 추출할 것.
  - **정렬 방향 확인**: "아이디 순" 조건은 기본 오름차순(`ORDER BY ID` 또는 `ASC`) 적용.
- **SQL 9일차 2번 (오프라인/온라인 리얼타임 상품 판매 데이터 통합하기)**:
  - **`=`과 `%` 와일드카드 혼용 오류**: `DATE_FORMAT(...) = '2022-03%'` 구문 오용 $\rightarrow$ `LIKE '2022-03%'` 또는 `DATE_FORMAT(SALES_DATE, '%Y-%m') = '2022-03'` 사용.
  - **존재하지 않는 컬럼 참조**: `OFFLINE_SALE` 테이블에는 `USER_ID` 컬럼이 없으므로 `NULL AS USER_ID`로 위치 및 스키마 일치 처리.
  - **`ORDER BY` 언더바 오타**: `PRODUCT_ID_ASC` 오타 $\rightarrow$ `PRODUCT_ID ASC` 띄어쓰기 적용.
- **SQL 11일차 (서브쿼리 조건 필터링 및 집계/문자열 결합)**:
  - **최다 작성자 추출 서브쿼리**: 리뷰 수가 가장 많은 회원 1명을 찾기 위해 서브쿼리 내에서 `GROUP BY MEMBER_ID ORDER BY COUNT(*) DESC LIMIT 1` 조합 적용.
  - **고유 회원 수 집계**: 년/월/성별 기준 구매 회원 수 집계 시 동일 사용자의 다회 구매를 하나로 세기 위해 `COUNT(DISTINCT USER_ID)` 필수 적용.
  - **문자열 결합 포맷팅**: 파일 경로 생성 시 `CONCAT('/home/grep/src/', BOARD_ID, '/', FILE_ID, FILE_NAME, FILE_EXT)` 함수를 활용하여 형식 준수.
- **SQL 12일차 2번 (저자 별 카테고리 별 매출액 집계하기)**:
  - **다중 테이블 조인 시 컬럼 소속 테이블 확인**: `AUTHOR_NAME`은 `AUTHOR` 테이블, 판매 날짜 `SALES_DATE`는 `BOOK_SALES` 테이블 등 각 테이블의 역할을 명확히 확인하고 별칭 매칭.
  - **날짜 필터링 와일드카드**: `YYYY-MM-DD` 형태의 문자열 날짜를 월 단위로 조회할 때는 단순 일치(`'2022-01'`)가 아닌 `LIKE '2022-01%'` 와일드카드 사용.

---

## 학습 현황

### Python 알고리즘 트랙
- **[1일차](python/1일차/1일차.ipynb)**: 데이터 처리 기초 | [오답노트](python/1일차/1일차_오답노트.md)
- **[2일차](python/2일차/2일차.ipynb)**: 웹 크롤링 정리 | [오답노트](python/2일차/2일차_오답노트.md)
- **[3일차](python/3일차/3일차.ipynb)**: 스택/큐 - [기능개발 (Lv.2)](https://school.programmers.co.kr/learn/courses/30/lessons/42586) | [오답노트](python/3일차/3일차_오답노트.md)
- **[4일차](python/4일차/4일차.ipynb)**: 스택/큐 6문제 진행 | [오답노트](python/4일차/4일차_오답노트.md)
- **[5일차](python/5일차/5일차.ipynb)**: 해시 4문제 진행 | [오답노트](python/5일차/5일차_오답노트.md)
- **[6일차](python/6일차/6일차.ipynb)**: 힙/정렬 3문제 진행 | [오답노트](python/6일차/6일차_오답노트.md)
- **[7일차](python/7일차/7일차.ipynb)**: 정렬/완전탐색 3문제 진행 | [오답노트](python/7일차/7일차_오답노트.md)
- **[8일차](python/8일차/8일차.ipynb)**: 완전탐색/Greedy 3문제 진행 | [오답노트](python/8일차/8일차_오답노트.md)
- **[9일차](python/9일차/9일차.ipynb)**: Greedy/정렬 3문제 진행 | [오답노트](python/9일차/9일차_오답노트.md)
- **[10일차](python/10일차/10일차.ipynb)**: Greedy/스택큐 3문제 진행 (큰 수 만들기, 같은 숫자는 싫어, 기능개발) | [오답노트](python/10일차/10일차_오답노트.md)
- **[11일차](python/11일차/11일차.ipynb)**: DFS/BFS/힙/Greedy 4문제 진행 (타겟 넘버, 더 맵게, 단속카메라, 게임 맵 최단거리) | [오답노트](python/11일차/11일차_오답노트.md)
- **[12일차](python/12일차/12일차.ipynb)**: 스택/큐, DFS/BFS, 완전탐색 3문제 진행 (주식가격, 네트워크, 카펫) | [오답노트](python/12일차/12일차_오답노트.md)
- **[13일차](python/13일차/13일차.ipynb)**: 해시, DFS/BFS, 완전탐색 3문제 세팅 (베스트앨범, 단어 변환, 전력망을 둘로 나누기) | [오답노트](python/13일차/13일차_오답노트.md)
- **[14일차](python/14일차/14일차.ipynb)**: 완전탐색, DFS/BFS, 힙 3문제 세팅 (피로도, 여행경로, 이중우선순위큐) | [오답노트](python/14일차/14일차_오답노트.md)
- **[15일차](python/15일차/15일차.ipynb)**: 완전탐색, DFS/BFS 2문제 진행 (모음사전, 무인도 여행) | [오답노트](python/15일차/15일차_오답노트.md)
- **[16일차](python/16일차/16일차.ipynb)**: 2차원 격자 탐색(DFS/BFS), 최단거리 2문제 세팅 (석유 시추, 미로 탈출) | [오답노트](python/16일차/16일차_오답노트.md)

### SQL 트랙
- **[1일차](sql/1일차/1일차.ipynb)**: GROUP BY & CASE WHEN 2문제 진행 | [오답노트](sql/1일차/1일차_오답노트.md)
- **[2일차](sql/2일차/2일차.ipynb)**: JOIN 2문제 진행 | [오답노트](sql/2일차/2일차_오답노트.md)
- **[3일차](sql/3일차/3일차.ipynb)**: NULL/GROUP BY 3문제 진행 | [오답노트](sql/3일차/3일차_오답노트.md)
- **[4일차](sql/4일차/4일차.ipynb)**: JOIN 1문제 진행 | [오답노트](sql/4일차/4일차_오답노트.md)
- **[5일차](sql/5일차/5일차.ipynb)**: JOIN 2문제 진행 | [오답노트](sql/5일차/5일차_오답노트.md)
- **[6일차](sql/6일차/6일차.ipynb)**: JOIN/GROUP BY 2문제 진행 | [오답노트](sql/6일차/6일차_오답노트.md)
- **[7일차](sql/7일차/7일차.ipynb)**: Lv.3~Lv.4 3문제 진행 (오랜 기간 보호한 동물(2), 식품분류별 가장 높은 가격, 취소되지 않은 진료 예약) | [오답노트](sql/7일차/7일차_오답노트.md)
- **[8일차](sql/8일차/8일차.ipynb)**: Lv.3~Lv.4 3문제 진행 (주문량이 많은 아이스크림들, 카테고리 별 도서 판매량, 대여 횟수가 많은 자동차) | [오답노트](sql/8일차/8일차_오답노트.md)
- **[9일차](sql/9일차/9일차.ipynb)**: Lv.3~Lv.4 2문제 진행 (헤비 유저가 소유한 장소, 오프라인/온라인 상품 판매 데이터 통합하기) | [오답노트](sql/9일차/9일차_오답노트.md)
- **[10일차](sql/10일차/10일차.ipynb)**: Lv.3~Lv.4 3문제 세팅 (우유와 요거트가 담긴 바구니, 없어진 기록 찾기, 5월 식품들의 총매출 조회하기) | [오답노트](sql/10일차/10일차_오답노트.md)
- **[11일차](sql/11일차/11일차.ipynb)**: Lv.3~Lv.4 3문제 진행 (그룹별 조건에 맞는 식당 목록 출력하기, 년 월 성별 별 상품 구매 회원 수, 조회수가 가장 높은 게시물 첨부파일) | [오답노트](sql/11일차/11일차_오답노트.md)
- **[12일차](sql/12일차/12일차.ipynb)**: Lv.3~Lv.4 2문제 진행 (조건에 맞는 사용자와 총 거래금액 조회하기, 저자 별 카테고리 별 매출액 집계하기) | [오답노트](sql/12일차/12일차_오답노트.md)
- **[13일차](sql/13일차/13일차.ipynb)**: Lv.3~Lv.4 2문제 세팅 (대여 기록이 존재하는 자동차 리스트 구하기, 서울에 위치한 식당 목록 출력하기) | [오답노트](sql/13일차/13일차_오답노트.md)
- **[기록장](sql/기록장/기록장.ipynb)**: 종합 SQL 연습장

---

*단순한 코테 기록용이 아닌 의미 있는 기록장이 되길 바라며*
